version: '3.1'

services:
  db:
    container_name: catbotdb
    image: postgres:alpine
    restart: always
    ports:
      - 5432:5432
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: catbot
  bot:
    build: .
    restart: always
    env_file:
      - catbot.env